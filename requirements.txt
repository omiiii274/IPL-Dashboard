# Core Shiny stack
shiny>=0.9.0
shinywidgets>=0.3.0
plotly>=5.18.0

# Data
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# ML
scikit-learn>=1.3.0
xgboost>=2.0.0        # optional — engine falls back to sklearn GBM if not installed
# lightgbm>=4.0.0     # alternative booster — uncomment to use instead of XGBoost
